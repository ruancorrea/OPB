{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}
{{/each}}

<h3>Editar gênero</h3>
<div class="card text-white bg-secondary mb-3">
    <div class="card-body">
        <form action="/admin/generos/edit" method="POST">
            <input type="hidden" name='id' value="{{genero._id}}">
            <label for="nome">Nome</label>
            <input type="text" id="nome" name="nome" class="form-control" value = "{{genero.nome}}">
            <label for="autor">Slug</label>
            <input type="text" id="autor" name="slug" class="form-control" value="{{genero.slug}}">
            <label for="genero">Gênero</label>
            <select name="genero" id="genero" class="custom-select">
                <option value="Narrativo">Narrativo</option>
                <option value="Lírico">Lírico</option>
                <option value="Dramático">Dramático</option>
            </select>
            <button type="submit" class="btn btn-success mt-3">Salvar</button>
        </form>
    </div>
</div>